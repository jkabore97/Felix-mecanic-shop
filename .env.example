# PostgreSQL (local : docker compose up -d ; Vercel : ajouté automatiquement par le Storage Neon/Postgres)
DATABASE_URL="postgresql://felix:felix@localhost:5432/felix"
# Secret de session (changer en production)
SESSION_SECRET="change-me-in-production"
# Photos : sur Vercel, créer un Blob Store (Storage → Blob) ; la variable est ajoutée automatiquement.
# BLOB_READ_WRITE_TOKEN=""
# En local, dossier de stockage des photos (par défaut ./data/uploads)
# UPLOAD_DIR="./data/uploads"
